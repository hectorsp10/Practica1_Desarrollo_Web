import React from 'react';
import NavBar from './NavBar';
function Header() {
    return (
        <header className="App-header">
            <NavBar />
        </header>
    );
}

export default Header;